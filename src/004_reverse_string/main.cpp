#include <iostream>

#include "utils/string_holder.h"

int main() {
    StringHolder stringHolder;
    stringHolder.showString();
    stringHolder.reverse();
    stringHolder.showString();
    return 0;
}
